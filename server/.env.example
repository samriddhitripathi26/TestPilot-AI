PORT=5000
NODE_ENV=development
JWT_SECRET=testpilot_super_secret_jwt_key_2026
# Optional MongoDB URI - if omitted or connection fails, TestPilot automatically uses the persistent local JSON database
MONGODB_URI=
# Gemini API Key - Optional: Can also be passed from the frontend UI or fallback to demo AI engine
GEMINI_API_KEY=
# Optional OpenAI API Key
OPENAI_API_KEY=
